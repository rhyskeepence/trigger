- dirs:
    - "src"
  files:
    - "**/*.hs"
  tasks: 
    - "stack build"
  run:
    - "stack exec"

- dirs:
    - "client"
  files:
    - "**/*.elm"
  ignore:
    - "Api.elm"
  tasks:
    - "elm-make"